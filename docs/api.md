# API Design

The API layer will expose structured endpoints for token discovery, wallet intelligence, liquidity summaries, and risk reports.

## Planned endpoints

- GET /health
- GET /tokens
- GET /tokens/:address
- GET /wallets/:address
- GET /analytics/risk/:tokenAddress
- GET /alerts
